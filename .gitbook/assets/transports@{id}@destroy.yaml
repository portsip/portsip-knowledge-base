post:
  tags:
    - Administration
  operationId: deleteTransport
  summary: Destroy transport
  description: |
    Destroy transport
  parameters:
    - name: id
      in: path
      required: true
      schema:
        $ref: "../components/schemas/transport.yaml#/id"
      description: The unique ID of the transport.
  responses:
    "204":
      description: No Content
    "4XX":
      description: Error
