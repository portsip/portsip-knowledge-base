post:
  tags:
    - Call Queue
  operationId: createCallQueueServer
  summary: Create a call queue server
  description: |
    Create a call queue server.  
    Please note that: at least one of ipv4 or ipv6 must be specified.
  requestBody:
    content:
      application/json:
        schema:
          type: object
          properties:
            name:
              $ref: "../components/schemas/queueserver.yaml#/name"
            enabled:
              $ref: "../components/schemas/queueserver.yaml#/enabled"
            ipv4:
              $ref: "../components/schemas/common.yaml#/ipv4"
            ipv6:
              $ref: "../components/schemas/common.yaml#/ipv6"
          required:
            - name
  responses:
    "201":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              id:
                $ref: "../components/schemas/queueserver.yaml#/id"
    "4XX":
      description: Error
get:
  tags:
    - Call Queue
  operationId: listCallQueueServers
  summary: List call queue servers
  description: |
    List call queue servers.
  parameters:
    - $ref: "../components/parameters/common.yaml#/filter"
    - $ref: "../components/parameters/common.yaml#/search"
    - $ref: "../components/parameters/common.yaml#/orderby"
    - $ref: "../components/parameters/common.yaml#/skip"
    - $ref: "../components/parameters/common.yaml#/top"
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              count:
                $ref: "../components/schemas/common.yaml#/count"
              items:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      $ref: "../components/schemas/queueserver.yaml#/id"
                    name:
                      $ref: "../components/schemas/queueserver.yaml#/name"
                    enabled:
                      $ref: "../components/schemas/queueserver.yaml#/enabled"
                    type:
                      $ref: "../components/schemas/queueserver.yaml#/type"
                    ipv4:
                      $ref: "../components/schemas/common.yaml#/ipv4"
                    ipv6:
                      $ref: "../components/schemas/common.yaml#/ipv6"
    "4XX":
      description: Error
