post:
  tags:
    - Call Pickup
  operationId: deleteCallPickupGroup
  summary: Delete a call pickup group
  description: |
    Delete a call pickup group.
  parameters:
    - name: id
      in: path
      required: true
      schema:
        $ref: "../components/schemas/callpickupgrp.yaml#/id"
      description: |
        The unique ID of the call pickup group.
  responses:
    "204":
      description: No Content
    "4XX":
      description: Error
