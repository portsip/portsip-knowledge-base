post:
  tags:
    - User
  operationId: kickOutMeetingParticipant
  summary: Kick out meeting participant
  description: |
    Kick out meeting participant.
  parameters:
    - name: id
      in: path
      required: true
      schema:
        $ref: "../components/schemas/confroom.yaml#/id"
      description: The unique ID of the meeting.
    - name: participant_id
      in: path
      required: true
      schema:
        $ref: "../components/schemas/confroom.yaml#/participant_id"
      description: The id of the participant.
  responses:
    "204":
      description: No Content
    "4XX":
      description: Error
